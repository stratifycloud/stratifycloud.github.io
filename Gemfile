# frozen_string_literal: true

source "https://rubygems.org"
gemspec

# Ruby 3.4+ dropped these from the default gems; jekyll/liquid/jekyll-spaceship
# still require them at runtime.
gem "bigdecimal"
gem "ostruct"

